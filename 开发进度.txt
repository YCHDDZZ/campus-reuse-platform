> 已完成 Spring Boot 后端框架、Spring Security 权限控制、MyBatis-Plus 数据访问层。
> 以及 Vue3、Element Plus 前端界面的开发；完成了平台核心业务数据库表结构设计，并实现了基于 Spring Cache 的缓存机制与 Redis 生产配置预留。
> 与此同时，MySQL生产环境配置已完成，具备切换部署能力。MinIO 相关依赖与配置已预留，但文件存储上传功能尚待进一步完善。

2026-03-17 数据库与 Navicat 配置进度
1. 已检查本机 MySQL80 服务状态，确认服务正在运行，可用于项目数据库管理。
2. 已确认本机 MySQL 可用连接信息为：主机 localhost，端口 3306，用户名 root，密码 root。
3. 已使用项目脚本 F:\Projects\campus-reuse-platform\backend\sql\schema-mysql.sql 初始化项目数据库 campus_reuse。
4. campus_reuse 数据库已创建完成，当前已导入 9 张核心业务表：platform_user、category、idle_product、product_favorite、product_message、trade_order、trade_review、product_report、donation_record。
5. 已同步修正后端生产环境数据库配置文件 F:\Projects\campus-reuse-platform\backend\src\main\resources\application-prod.yml，将 MySQL 密码改为本机实际可用的 root，保证后端配置与本地 MySQL 环境一致。
6. 已为 Navicat 写入项目数据库连接配置，连接名称为 campus_reuse，连接到本机 MySQL 服务，后续可直接用于数据库建表、查看数据、维护结构与执行 SQL。
7. 当前 Navicat 连接参数如下：
   - 连接名称：campus_reuse
   - 数据库类型：MySQL
   - 主机：localhost
   - 端口：3306
   - 用户名：root
   - 密码：root
   - 项目数据库：campus_reuse
8. 如 Navicat 在配置写入前已经处于打开状态，关闭后重新打开一次，即可在左侧连接列表中看到 campus_reuse 连接并直接管理该项目数据库。
2026-03-17 MySQL 实际运行切换进度
1. 已将后端默认运行环境从 demo 切换为 prod，当前项目默认直接连接本机 MySQL 数据库 campus_reuse。
2. 已将 application-prod.yml 的数据库账号统一为 root/root，并将本机无 Redis 服务情况下的缓存类型临时调整为 simple，保证系统可稳定启动。
3. 已确认项目启动后自动向 MySQL 写入演示数据，当前 MySQL 中已有 3 个用户、4 个分类、3 个商品。
4. 已通过接口实际验证 MySQL 写入成功：使用 student 账号登录后，已新增 1 条收藏记录和 1 条订单记录；现在在 Navicat 的 campus_reuse 库中可直接查看 product_favorite 与 trade_order 表数据变化。
5. 后续前端的登录、收藏、下单、留言、评价等操作，都会直接写入 MySQL，而不是写入 H2 内存库。